Actividad Entornos de Desarrollo

Aprender funciones basicas de GIT

- se creo el repositorio con "git init"
- configure el proyecto con mis datos con "git config user.name" y "git user.email"
- cree un archivo de texto con "touch" 
- añadi el archivo al repositorio con "git add historia.txt"
- añadi comentarios el archivo con "git commit"
- hice "git checkout" para volver an punto especifico del proyecto
- añadi el repositorio a GITHUB con "git remote add origin url"
- cree una rama del proyecto llamada editores con "git checkout -b 'editores'"
- realice modificaciones al primer archivo creado y cree un nuevo archivo de texto
- utilice el comando "git merge editores" para poder unir las ramas al master
- se creo un conflicto al editar el mismo archivo en ambas ramas
- se corrigio el conflicto con "git merge"
- se creo tag a las dos versiones del proyecto con "git tag"
- se añadio un README
- se hizo un PUSH para sincronizar con el repositorio local y online
- seguimos trabajando.....
